<?xml version="1.0" encoding="utf-8" ?>
  <LogsConfig>
    <!-- 日志相关配置项 Begin -->
    <!-- 日志级别 All|DEBUG|INFO|WARN|ERROR|FATAL -->
    <_LOG_LEVEL_>All</_LOG_LEVEL_>
    <!--是否按照日志级别进行分类输出-->
    <_LOG_LEVELGROUP_>FALSE</_LOG_LEVELGROUP_>
    <!-- 日志文件路径(相对路径或绝对路径)+文件名（不含文件扩展名）（默认NLogger） -->
    <_LOG_FILE_>Logs\\Log</_LOG_FILE_>
    <!-- 日志文件的最大长度（单位：字节）（不得小于 1024字节，即 1K）（默认1M = 1*1024*1024=1048576） 当日志文件超过该大小时会自动重建 -->
    <_LOG_FILE_MAX_SIZE_>10485760</_LOG_FILE_MAX_SIZE_>
    <!-- 日志文件是否按命名空间分开存储的配置节关键字: TRUE|FALSE （默认FALSE以时间命名） -->
    <_LOG_SPLIT_>FALSE</_LOG_SPLIT_>
    <!-- 当日志文件按命名空间分开存储时（即_LOG_SPLIT_=TRUE），命名空间长度最大截取到第几分段（以“.”分割）(该值必须大于0，方有效)，(可选)默认命名空间全称 -->
    <_LOG_NS_DIV_COUNT_>3</_LOG_NS_DIV_COUNT_>
    <!-- 异步写入日志标示，TRUE：新启线程采用日志队列方式异步写入，FALSE：直接写入日志文件（默认FALSE） -->
    <_LOG_ASYN_>TRUE</_LOG_ASYN_>
    <!-- 异步写入时，当日志队列为空的等待毫秒数（必须大于等于0，为0则没有等待时间(较耗资源)；可不配置，默认100毫秒）（_LOG_ASYN_必须配置TRUE，该项才起作用） -->
    <_LOG_ASYN_WAIT_>100</_LOG_ASYN_WAIT_>
    <!-- 异步写入线程是前台线程还是后台线程（对应Thread.IsBackground属性），TRUE后台线程，FALSE前台线程（默认FALSE）（_LOG_ASYN_必须配置TRUE，该项才起作用） -->
    <_LOG_ASYN_THREAD_BACKGROUND_>TRUE</_LOG_ASYN_THREAD_BACKGROUND_>
    <!-- #日志文件的保存时间（单位：天，整数-默认30天）（值大于零方有效） -->
    <_LOG_FILE_SAVE_TIME_>30</_LOG_FILE_SAVE_TIME_>
    <!-- 日志相关配置项 End -->
  </LogsConfig>